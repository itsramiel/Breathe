import { Dimensions } from "react-native";

export const { width: SCREEN_WIDTH, height: SCREEN_HEIGHT } = Dimensions.get("window");

export const CENTER = { x: SCREEN_WIDTH / 2, y: SCREEN_HEIGHT / 2 };

export const COLOR = "#40919e";
